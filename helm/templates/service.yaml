apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-boilerplate.fullname" . }}
  labels:
    {{- include "app-boilerplate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-boilerplate.selectorLabels" . | nindent 4 }}
